# GPU-side dependencies — needed only for the generation step (running the
# models). Install these on the USC cluster or Colab, on top of requirements.txt.

# Inference engine. Continuous batching + prefix caching (design/07 §7.2).
# 0.10.x is the last 0.10 series on the torch 2.x / CUDA 12.x line and is what
# the Colab/cluster images of this era ship; pin exactly at pre-registration.
vllm==0.10.0

# Model loading / tokenizers. Held in the 4.x line for compatibility with the
# pinned vLLM above; the 5.x series is a breaking change and is deliberately
# avoided here. (accelerate and bitsandbytes were removed 2026-07-20: no HF
# fallback engine exists in src/ — VllmEngine is the only backend — so neither
# package is imported anywhere; vLLM's own dependency resolution brings in
# what it needs.)
transformers==4.53.2

